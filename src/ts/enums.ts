export enum CONTEXT {
  UPDATE_LANGUAGE,
}

export enum LANGUAGES {
  BR = "BR",
  EN = "EN",
  ES = "ES",
}

export enum PLANS {
  BASIC = "BASIC",
  ADVANCED = "ADVANCED",
  SUBSCRIPTION = "SUBSCRIPTION",
}

export enum FREQUENCY {
  SINGLE = "SINGLE",
  MONTHLY = "MONTHLY",
}

export enum SURVEY {
  SINGLE = "SINGLE",
  MULTIPLE = "MULTIPLE",
  TEXT = "TEXT",
  MULTILINE = "MULTILINE",
}

export enum METHOD {
  SMS = "SMS",
  EMAIL = "EMAIL",
  NONE = "NONE",
}

export enum ALERT {
  ERROR = "ERROR",
  INFO = "INFO",
  WARNING = "WARNING",
  SUCCESS = "SUCCESS",
}
